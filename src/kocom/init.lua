return {
  parser = require "kocom.parser",
  protocol = require "kocom.protocol",
  registry = require "kocom.registry",
  session = require "kocom.session",
  transport = require "kocom.transport",
  mappings = require "kocom.mappings",
}
